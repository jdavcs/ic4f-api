<nav class="ic4f-nav">

<% 
// This constructs the menu html.
// TODO: This code belongs in the controller.

const items = [
  {'title': 'blog', 'url': '/'},
  {'title': 'projects', 'url': '/projects/'},
  {'title': 'about', 'url': '/about/'},
  {'title': '.edu', 'url': 'http://sergey.cs.uni.edu'},
  {'title': 'github', 'url': 'http://github.com/ic4f'},
];

let html = [];
for (let i=0; i<items.length; i++) {
  let active = '';
  if (parentMenu === i) {
    active = 'class="active"';
  } else {
    active = '';
  }
  html.push(`<a href="${items[i]['url']}" ${active} >${items[i]['title']}</a>`);
}
%>

<%- html.join('') %> 

</nav>
